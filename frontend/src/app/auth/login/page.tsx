export const dynamic = 'force-dynamic';
'use client';import {useState} from 'react';import {api,saveAuth} from '@/lib/api';import {Logo} from '@/components/Logo';
export default function Login(){const [email,setEmail]=useState('admin@destinationafrica.com');const [password,setPassword]=useState('Admin123!');const [err,setErr]=useState('');async function submit(e:any){e.preventDefault();try{const r=await api.login(email,password);saveAuth(r);location.href=r.user.role==='admin'?'/dashboard/admin':r.user.role==='operator'?'/dashboard/operator':'/dashboard/traveler'}catch(x:any){setErr('Login failed. Check credentials.')}}return <main className="grid min-h-screen place-items-center bg-sand/40 p-6"><form onSubmit={submit} className="card w-full max-w-md p-8"><Logo/><h1 className="mt-8 text-3xl font-black">Login</h1><input className="input mt-5" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input mt-3" value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password"/><button className="btn-primary mt-5 w-full">Login</button>{err&&<p className="mt-3 text-red-600">{err}</p>}<div className="mt-6 rounded-2xl bg-sand p-4 text-xs"><b>Demo:</b><br/>Admin: admin@destinationafrica.com / Admin123!<br/>Operator: operator@destinationafrica.com / Operator123!<br/>Traveler: traveler@destinationafrica.com / Traveler123!</div><a className="mt-4 block text-sm font-bold" href="/auth/register">Create account</a></form></main>}